# Optional. Point the theme at a Strapi instance to source the Features and
# Portfolio collections from it at build time. Leave unset to build from the
# JSON snapshot in src/data/.
STRAPI_URL=http://localhost:1337

# Read-only API token from Settings > API Tokens in the Strapi admin.
# Not needed if the two collections are publicly readable.
STRAPI_TOKEN=
